  public ArrayList<Integer> kLargest(int[] arr, int k) {

 Arrays.sort(arr);
        ArrayList<Integer> li = new ArrayList<>();
        for(int i=0;i<k;i++){
            li.add(arr[arr.length-1-i]);
        }
        
        return li;

}
