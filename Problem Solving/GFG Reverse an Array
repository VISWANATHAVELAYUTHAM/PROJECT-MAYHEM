class Solution {
    public void reverseArray(int arr[]) {
        // code here
        for(int i=0, j=arr.length-1; i<=j; i++, j--) {
            int temp = arr[i];
            arr[i] = arr[j];
            arr[j] = temp;
        }
    }
}
